#ifndef DETAILREPORT_H
#define	DETAILREPORT_H
#include<vector>
class DetailReport {
public:
    std::vector<unsigned> s;
    std::vector<unsigned> d;
    std::vector<unsigned> day;
    std::vector<unsigned> month;
    std::vector<unsigned> year;
    std::vector<unsigned> ii;
    std::vector<unsigned> stratum;
    std::vector<unsigned> x;
    std::vector<unsigned> u;
    std::vector<unsigned> initInf;
    std::vector<unsigned> vac;
    std::vector<unsigned> prot;
    std::vector<unsigned> state;
    std::vector<unsigned> dayInf;
    std::vector<unsigned> dNFARI;
    std::vector<unsigned> visit;
};

#endif	/* DETAILREPORT_H */
