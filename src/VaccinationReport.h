#ifndef VACCINATIONREPORT_H
#define	VACCINATIONREPORT_H
#include<vector>
class VaccinationReport {
public:
    std::vector<unsigned> s;
    std::vector<unsigned> mm;
    std::vector<unsigned> month;
    std::vector<unsigned> year;
    std::vector<unsigned> k;
    std::vector<unsigned> x;
    std::vector<unsigned> u;
    std::vector<unsigned> number;
    std::vector<unsigned> newly;
};

#endif	/* VACCINATIONREPORT_H */